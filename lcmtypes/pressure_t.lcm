package bmp_lcm;

struct pressure_t
{
  double time;        // epoch time, in microseconds [us]
  double temperature; // temperature, in degrees centigrade [degC]
  double pressure;    // pressure, in hectopascal [hPa]
}